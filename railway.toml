[build]
buildCommand = "composer install --no-dev --optimize-autoloader --no-interaction"

[build.php]
extensions = ["gd", "opcache", "pdo_mysql", "redis", "sodium"]

[deploy]
startCommand = "php artisan serve --host=0.0.0.0 --port=${PORT:-8000}"
